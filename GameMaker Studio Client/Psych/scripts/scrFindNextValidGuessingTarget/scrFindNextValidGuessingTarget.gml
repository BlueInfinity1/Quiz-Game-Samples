///scrFindNextValidGuessingTarget(currentGuessingTarget)

//show_message("Setting next guess target, starting search from currentGuessingTarget")
/*with objGameController
{
	for (var i = argument0; i < playerArraySize; i += 1)
	{
		if !players[i,playerData.isDead]
		{
			guessingTargetIndex = i
			return i
		}
	}
	
	//if we've gone through the whole array and haven't found the guessing target,
	//then return playerCount
	return playerArraySize
	
}*/